{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "Default": "Server=(localdb)\\MSSQLLocalDB;Database=AmaZounDB;Trusted_Connection=True;MultipleActiveResultSets=true"
  },
  "Jwt": {
    "Key": "The super long JWT secret Key because it must be hard to find the super long JWT secret key",
    "Issuer": "MiniMarket API",
    "Audience": "MiniMarket Client"
  }
}
